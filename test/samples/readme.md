This test harness uses Selenium to exercise the web-based sample apps. To compile and run it, please first install Maven. You can then build the tests using:

    mvn package

You can run the tests using:

    mvn exec:java
