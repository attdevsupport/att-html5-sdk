Ext.define('KitchenSink.view.testPairwiseonly', {
    extend: 'Ext.Container',
    xtype: 'attTestPairwise',

	requires: [
       'Att.Provider',
	   'KitchenSink.view.config'
    ],
	
    config: {
        provider: undefined,
        title: 'Test Pairwise',
        layout: {
            type: 'vbox',
            pack: 'center',
            align: 'center'
        },
        items: {
            xtype: 'container',
            width: '80%',
           items:[{
                xtype: 'fieldset',
                defaults: {
                    labelWidth: 120
                },
                items: [

                ]
            }, {
                xtype: 'button',
                text: 'Test Pairwise',
                action: 'attTest',
                style: 'margin-bottom: 10px;'
            }]
        },
        control: {
            'button[action=attTest]': {
                tap: 'onPush'
            }
        }
    },
	
	onPush: function() {
        provider = Ext.create('Att.Provider',{
			apiBasePath: '/att'
        });
		var cfg = KitchenSink.view.config;
		Ext.Viewport.hide();
		//pairwiseSMSTests(cfg);
		//pairwiseMMSTests(cfg);
		//pairwiseDCTests(cfg);
		//pairwiseMoboTests(cfg);
		//pairwiseMimTests(cfg);
	}
});
